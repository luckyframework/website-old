---
title: Generating Test Data with Boxes
order: 11
intro: >
  LuckyRecord::Box provides a nice way to generate test data in a flexible
  way. Boxes are objects with a minimal, but powerful API.
---

## Creating a Box

```
# spec/support/boxes/post_box.cr
class PostBox < LuckyRecord::Box
  def initialize
    title “My Post”
    body “Test Body”
  end
end
```

## Saving records

```
PostBox.create
PostBox.create_pair
```

### Overriding data

```
PostBox.create &.title(“Draft”).body(“custom body”)
PostBox.create_pair &.title(“Draft”)
```
